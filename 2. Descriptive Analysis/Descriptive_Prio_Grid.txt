Zeile 1-30 : 
Hier wird der ICEWS Datensatz eingelesen und die Polygons auf dem Shellship order, der  von 
der Priogrid website runtergeladen wurden. Dieser enthält Polygons zur gesamten Welt.Außerdem with der Path definiert indem alle Plots gespeichert werden.

Zeile 30-68: 
Hier wird ein Datensatz aus den cellshp package runtergeladen, der alle Polygons in Africa
enthält und ebenfalls informationen über die Hauptstädte der jeweiligen Länder enthällt.                Wenn man das sf object mit den heruntergeladenen Polygons megend, dann enthält man nur die
Polygons in Africa. Das ist unsere Africa Map. (12378 observations)
             
Zeile 59:    
Diese Zeile enthält die Fehlermeldung. St_join not found. Diese muss aber unbedingt 
ausgeführt werden weil die nächste sonst nicht ausführbar ist. R muss annehmen dass die
geocoordinaten planar sind. Das gesschieht in dieser Zeile mit dem Befehel sf_use_s2(FALSE)

Zeile 70-83: 
Wenn man obigen Datensatz mit den Polygons in Africa und den infos zu den Hauptstädten, von
ein sf object in ein Dataframe umwandelt, dann verschwindet die geometry variable, und es
bleiben nur noch die geocoordinaten der Hauptstädte übrig. Wenn man diese dann mit den 
heruntergeladenen Polygons mergend, erhält man nur die Polygons in dennen die Hauptstadt enthalten ist
(52 observations)

Zeile 96-106:
Hier werden die Grenzen der Städte extrahiert, damit in den Graphiken mit der Afrika Karte, die Grenzen 
über die Polygone gezeichnet werden können. Dies geschieht mit der Funktion Borders, die auf die Karte
"world" zugreift. Diese Karte ist jedoch veraltert, es gibt evtl einen Weg dass mit einen anderen Package zu machen. Da wir nicht die ganze Welt benötigen wie hier nocheinmal nach den Ländern gefiltert, die tatsächlich auch in Afrika liegen. 

Zeile 108-126:
Hier wird der Datensatz erstellt.
priogrid | n.x: Anzahl in priogrid |n.y: Summe über Anzahl in allen priogrids in einem Land |rel: 
Relative Anzahl n.x/n.y

Zeile 130-145:
Afrika Karte mit Relativer Anzahl in jedem Polygon über alle Jahre

Zeile 150-165:
Afrika Karte mit Absoluter Anzahl in jedem Polygon über alle Jahre

Zeile 169-185:
Afrika Karte nur mir Polygons, die mehr als 1% der Events in einem Land enthalten über alle Jahre hinweg

Zeile 201-2012:
Namen der Länder für die Map aus dem celshp Package sind nicht dieselben wie in unseren icews Datensatz, deswegen werden sie für den Datenasatz aus dem celshp package angepasst. 

Zeile 216-238:
Hier wird wieder ein Datensatz erstellt wie in Zeilen 108-126 nur für Somalia über alle Jahre hinweg. Man kann es auch nach dem Jahr filter, das in diesem Fall hier auskommentiert ist. 

Zeile 239:
Für das Balkendiagram wird aus dem cellship Datensatz nur der das Grid der Hauptstadt von Somalia benötigt. Die Grids der anderen Hauptstädte werden nicht benötigt, deswegen wird hier danach gefiltert. 

Zeile 243-255: 
Balkendiagram der Relativen Häufigkeit der Grids in Somalia. Es sind so viel mehr Grids als in der letzten Präsenation weil es über alle Jahre hinweg ist. Wahrscheinlich ist es hier plausibler sich auf ein Jahr zu begrenzen. 

Zeile 261-267: 
Um den Priogrid Plot für nur ein Land zu erstellen, müssen wir alle anderen Polygons in Africa, die nicht Teil dieses Landes sind entfernen. Dies geschiet hier. 

Zeile 269-280: 
Graphik: Afrika karte mit Relativer Anzahl der Events in jedem polygon nut für Somalia. 